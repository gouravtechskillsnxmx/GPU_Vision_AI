# Web
fastapi
uvicorn[standard]
python-multipart

# Core numeric / vision
numpy
opencv-python-headless

# OCR
paddleocr
paddlepaddle

# Database (Postgres, psycopg v3)
psycopg[binary]

# Optional but safe (often pulled transitively, we pin it)
protobuf<5
